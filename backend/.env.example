PORT=3001
NODE_ENV=development

# Optional: Add database URL when you migrate from in-memory storage
# DATABASE_URL=postgresql://user:password@localhost:5432/webhooks

# Optional: Default webhook secret
# DEFAULT_WEBHOOK_SECRET=your-default-secret-key
